steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-f"
      - "backend-dotnet/Dockerfile"
      - "-t"
      - "asia-south1-docker.pkg.dev/$PROJECT_ID/textzy/textzy-backend:$COMMIT_SHA"
      - "-t"
      - "asia-south1-docker.pkg.dev/$PROJECT_ID/textzy/textzy-backend:latest"
      - "backend-dotnet"

images:
  - "asia-south1-docker.pkg.dev/$PROJECT_ID/textzy/textzy-backend:$COMMIT_SHA"
  - "asia-south1-docker.pkg.dev/$PROJECT_ID/textzy/textzy-backend:latest"

options:
  logging: CLOUD_LOGGING_ONLY
